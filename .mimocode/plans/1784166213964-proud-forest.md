# Premium Editorial UI Redesign Plan

## Overview

Redesign the landing page, login page, and register page using the premium-editorial-ui methodology. Shift to a warm editorial color palette, extract landing page components, and implement split-panel auth layouts.

---

## Phase 1: Design System Tokens (`globals.css`)

**File:** `app/globals.css`

Update the `@theme inline` block to the warm editorial palette:

| Token | Current | New |
|-------|---------|-----|
| `--color-background` | `#fafafa` | `#F8F5F1` |
| `--color-foreground` | `#09090b` | `#2E2B29` |
| `--color-primary` | `#5b4bff` | `#2E2B29` |
| `--color-primary-light` | `#7c6fff` | `#4A4541` |
| `--color-primary-dark` | `#4a3de0` | `#1A1816` |
| `--color-accent` | `#8b5cf6` | `#C5A07A` |
| `--color-accent-light` | `#a78bfa` | `#D4B896` |
| `--color-muted` | `#71717a` | `#8A8580` |
| `--color-muted-foreground` | `#a1a1aa` | `#B5B0AB` |
| `--color-border` | `#e4e4e7` | `#E8E3DD` |
| `--color-card` | `#ffffff` | `#FFFFFF` |
| `--color-card-hover` | `#f4f4f5` | `#F2EDE8` |
| `--color-surface` | `#f4f4f5` | `#F0EBE5` |

Add Google Fonts for editorial typography:
- `Cormorant Garamond` — headings
- `Inter` — body (keep existing)

Update dark mode variables to match warm editorial dark tones.

Update `text-gradient` utility to use accent gold gradient.

---

## Phase 2: Root Layout (`app/layout.tsx`)

**File:** `app/layout.tsx`

- Add `Cormorant Garamond` font import alongside Inter
- Set CSS variables for both fonts
- Update body class to use the new font stack

---

## Phase 3: Landing Page Component Extraction

Extract each section into its own file under `components/landing/`:

### 3a. `components/landing/Hero.tsx`
- Editorial hero with large whitespace
- Cormorant Garamond heading at massive scale (text-5xl to text-8xl)
- Refined subtitle in Inter
- Gold accent on key text
- Minimal CTA with thin border button style
- Soft scroll indicator
- Remove gradient blurs, use clean warm background

### 3b. `components/landing/Features.tsx`
- Editorial card grid with large spacing
- Remove colorful gradient icon backgrounds
- Use simple line icons or muted tone icons
- Warm card backgrounds with subtle borders
- Generous padding and breathing room

### 3c. `components/landing/HowItWorks.tsx`
- Clean numbered steps with editorial typography
- Remove colorful step backgrounds
- Use accent gold for step numbers
- Elegant horizontal flow with thin connecting line

### 3d. `components/landing/FAQ.tsx`
- Refined accordion with warm borders
- Clean typography hierarchy
- Subtle hover states

### 3e. `components/landing/CTA.tsx`
- Minimal, spacious CTA section
- Large heading in Cormorant Garamond
- Refined button with thin border

### 3f. `components/landing/index.ts`
- Barrel export for all landing components

---

## Phase 4: Landing Page (`app/(landing)/page.tsx`)

**File:** `app/(landing)/page.tsx`

- Import extracted components from `components/landing/`
- Replace inline definitions with component imports
- Reduce from ~466 lines to ~30 lines

---

## Phase 5: Navbar Redesign (`components/layout/Navbar.tsx`)

**File:** `components/layout/Navbar.tsx`

- Update logo to use warm tones
- Refine navigation link typography
- Thin border bottom on scroll instead of glass blur
- Elegant hover states with underline animation
- Gold accent on CTA button

---

## Phase 6: Footer Redesign (`components/layout/Footer.tsx`)

**File:** `components/layout/Footer.tsx`

- Warm background with subtle top border
- Editorial typography for section headers
- Refined link styles
- Clean minimal layout

---

## Phase 7: Auth Layout (`app/(auth)/layout.tsx`)

**New file:** `app/(auth)/layout.tsx`

Create a shared auth layout with:
- Split-panel design (image left, form right)
- Left panel: warm background with editorial branding, tagline, and subtle pattern/imagery
- Right panel: clean white form area
- Mobile: stack vertically with image on top
- Suitora logo and navigation back to landing

---

## Phase 8: Login Page (`app/(auth)/login/page.tsx`)

**File:** `app/(auth)/login/page.tsx`

- Remove standalone header (handled by auth layout)
- Refine form with editorial typography
- Cormorant Garamond heading
- Warm-toned input fields with subtle borders
- Gold accent on primary button
- Refined link styles

---

## Phase 9: Register Page (`app/(auth)/register/page.tsx`)

**File:** `app/(auth)/register/page.tsx`

- Same split-panel treatment as login
- Consistent form styling
- Warm editorial typography

---

## Phase 10: UI Component Refinement

### `components/ui/Button.tsx`
- Add `variant="editorial"` with thin border, warm tones
- Refine hover animations (subtle lift, not scale)
- Gold accent for primary actions

### `components/ui/Input.tsx`
- Warm border colors
- Refined focus ring in accent gold
- Subtle background tint

---

## Implementation Order

1. `globals.css` — design tokens (everything depends on this)
2. `app/layout.tsx` — font setup
3. `components/ui/Button.tsx` — button variants
4. `components/ui/Input.tsx` — input styling
5. `components/landing/Hero.tsx` — extract + redesign
6. `components/landing/Features.tsx` — extract + redesign
7. `components/landing/HowItWorks.tsx` — extract + redesign
8. `components/landing/FAQ.tsx` — extract + redesign
9. `components/landing/CTA.tsx` — extract + redesign
10. `components/landing/index.ts` — barrel export
11. `app/(landing)/page.tsx` — use extracted components
12. `components/layout/Navbar.tsx` — redesign
13. `components/layout/Footer.tsx` — redesign
14. `app/(auth)/layout.tsx` — new split-panel layout
15. `app/(auth)/login/page.tsx` — redesign
16. `app/(auth)/register/page.tsx` — redesign

---

## Verification

1. Run `npm run dev` and verify all pages render
2. Check landing page at `/` — all 5 sections visible, responsive
3. Check login at `/login` — split-panel layout, form works
4. Check register at `/register` — split-panel layout, form works
5. Test responsive behavior at mobile/tablet/desktop breakpoints
6. Verify no TypeScript errors with `npx tsc --noEmit`
7. Check accessibility: focus states, contrast ratios, semantic HTML
8. Verify animations are smooth and not excessive

---

## Files Modified

- `app/globals.css` — design tokens
- `app/layout.tsx` — font imports
- `app/(landing)/page.tsx` — simplified imports
- `app/(auth)/login/page.tsx` — redesigned
- `app/(auth)/register/page.tsx` — redesigned
- `components/ui/Button.tsx` — editorial variant
- `components/ui/Input.tsx` — warm styling
- `components/layout/Navbar.tsx` — redesigned
- `components/layout/Footer.tsx` — redesigned

## Files Created

- `components/landing/Hero.tsx`
- `components/landing/Features.tsx`
- `components/landing/HowItWorks.tsx`
- `components/landing/FAQ.tsx`
- `components/landing/CTA.tsx`
- `components/landing/index.ts`
- `app/(auth)/layout.tsx`
